import React, { useState } from "react";
import axios from "axios";
import { useNavigate } from "react-router-dom";

function CreateManageRoutine() {
    const ManageRoutinePage = () => {
      return <div>여기는 루틴 관리 생성 페이지입니다</div>;
    };

}

export default CreateManageRoutine;   // export default로 컴포넌트 내보내기